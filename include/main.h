#ifndef __MAIN_H
#define __MAIN_H

#include "raisim/World.hpp"
#include "raisim/RaisimServer.hpp"
#include "raisim/math.hpp"
#include "opmath.h"

// #define period 200
// #define Run_gait 0

// double T = 0.4;
// double dt = 0.001;
// float Hf = 0.15;
// float Hb = 0.3;
// float swing_time = 0.3;

// float tf = 0;
// float p0 = 0;
// float pf = 0;
// float vf = 0;
// float v0 = 0;

// Angle angle[2];
// Angle angleV[2];
// Position position[2];
// Position velocity[2];

// std::vector<float> x_position;
// std::vector<float> z_position;

// Eigen::VectorXd co;
// Eigen::VectorXd vel;
// Eigen::VectorXd force(3);
// Eigen::MatrixXd R;

// raisim::Vec<3> bodyPosition, bodyVelocity;

// void set_zero(void);
// void simple_set(float xx,float yy,float zz);
// void set_position(Leg leg,float xx,float yy,float zz);
// void setpoint(const raisim::VecDyn & pos,const raisim::VecDyn & vel,float x,float y,float z);
// void create_gait(void);
// Eigen::VectorXd tau(Eigen::VectorXd pA,Eigen::VectorXd vA,Eigen::VectorXd pT,Eigen::VectorXd vT,Eigen::VectorXd pG,Eigen::VectorXd dG);
// Eigen::VectorXd pos2Eig(Position & pos);
// Eigen::VectorXd calcu_endpoint_pos(int k);
#endif